{
  "name": "@chesscoach/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The analysis, statistics and storage layer — no Node, no DOM, so it runs on a server or in a phone.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "chess.js": "^1.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}

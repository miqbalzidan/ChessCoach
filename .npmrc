# A Node version below what a dependency demands is otherwise only an install-time
# warning, and better-sqlite3's native module then segfaults mid-test — which is a
# miserable way to discover the real cause. Fail at install, where the message is clear.
engine-strict=true

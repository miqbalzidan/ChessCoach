node_modules/
dist/
*.tsbuildinfo
# Exported snapshots are build output: one per export, a megabyte each.
# The fonts under web/public/fonts are vendored on purpose, so a build needs no network.
exports/
*.local
.env
.env.*
!.env.example
# Anchored patterns would only cover the repo root; the database lives in
# server/data, so match the files wherever they are.
*.db
*.db-journal
*.db-wal
*.db-shm
.DS_Store
*.log
.vite/
coverage/
